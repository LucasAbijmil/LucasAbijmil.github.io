source "https://rubygems.org"
# To view the changes please save your work (⌘S) and reload the page (⌘R)
#     bundle exec jekyll serve
#
# Run this command line to preview the website with automatic reload
# To view the changes please save your work (⌘S), the reload will be automatic
#     bundle exec jekyll serve --livereload
#
# Notice that if you have modified the _config.yml file, you have to relaunch the commands above.

# If you want to use GitHub Pages, remove the "gem "jekyll"" below
# gem "jekyll", "~> 4.2.0"


# This is the default theme for new Jekyll sites. You may change this to anything you like.

# The theme is commented because we use our proper theme (based on minima !)
# So we now using a reguler theme insted of a Gem-based theme
# gem "minima", "~> 2.5"


# If you're using Github Pages, uncomment the line below.
# For update Github Pages please run : `bundle update github-pages`
gem "github-pages", group: :jekyll_plugins

# If you have any plugins, put them here!
group :jekyll_plugins do
  gem "jekyll-feed", "~> 0.12"
end

# Windows and JRuby does not include zoneinfo files, so bundle the tzinfo-data gem
# and associated library.
platforms :mingw, :x64_mingw, :mswin, :jruby do
  gem "tzinfo", "~> 1.2"
  gem "tzinfo-data"
end

# Performance-booster for watching directories on Windows
gem "wdm", "~> 0.1.1", :platforms => [:mingw, :x64_mingw, :mswin]